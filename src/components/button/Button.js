import React from 'react';

import './button.scss';

const Button = () => {
    return (
        <div>
            <button>
                Войти как администратор
            </button>
        </div>
    );
};

export default Button;